<Window x:Class="pdf2word.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
Title="pdf2word — bx211" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Button Name="BtnOpen" Width="120" Margin="0,0,10,0">Open PDF</Button>
            <Button Name="BtnConvert" Width="140">Convert to Word</Button>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#CCC" BorderThickness="1" CornerRadius="6" Background="#FFF">
            <TextBox Name="TxtDrop" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" AllowDrop="True" IsReadOnly="True"/>
        </Border>

        <StatusBar Grid.Row="2" Margin="0,8,0,0">
            <StatusBarItem>
                <TextBlock Name="StatusText">Ready</TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
